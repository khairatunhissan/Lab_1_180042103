// __dirname= path of the current direcyory

const currentdirectory = __dirname;
console.log(currentdirectory)

//__filename=filename

console.log(__filename);

//require()=function to use modules

const fs= require("fs");
console.log(fs);

//module = info about the module

console.log(module);

//process =info about where the process is being executed

console.log(process);